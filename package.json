{
  "name": "swipre-usuarios",
  "version": "1.0.0",
  "private": true,
  "description": "Proyecto aislado para probar RF-002 (Gestionar usuarios y asignar rol) del sistema MOPGIMED/SIPGIMED, sin depender de la base de datos ni del backend completo.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:postman": "newman run postman/swipre-usuarios.postman_collection.json -e postman/swipre-usuarios.postman_environment.json"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "vitest": "^2.1.5",
    "@playwright/test": "^1.48.0",
    "newman": "^6.2.1"
  }
}
